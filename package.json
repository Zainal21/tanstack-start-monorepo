{
  "name": "tanstack-start-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Type-safe monorepo blueprint: TanStack Start frontend + Elysia API + Postgres",
  "license": "MIT",
  "author": "Muhamad Zainal Arifin",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "validate": "bun run format:check && bun run lint && bun run check-types && bun run test && bun run build",
    "security:audit": "bun audit --audit-level=high",
    "db:up": "docker compose up -d --wait",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:seed": "turbo run db:seed",
    "db:setup": "bun run db:migrate && bun run db:seed",
    "db:studio": "turbo run db:studio"
  },
  "devDependencies": {
    "oxfmt": "^0.68.0",
    "turbo": "^2.11.2",
    "typescript": "^7.0.2"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "version": "1.3.14"
    }
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "packageManager": "bun@1.3.14"
}
